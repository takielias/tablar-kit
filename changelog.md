# Changelog

All notable changes to `TablarKit` will be documented in this file.

## Unreleased

### Fixed
- `tablar-kit:install` no longer adds the unused `react-filerobot-image-editor`, pins `filerobot-image-editor` to `~4.8.1`, and writes a `package.json` `overrides` block for `react`, `react-dom`, and `react-konva` so the FilePond image-edit pen icon stops throwing `Invalid hook call` / `react-konva 19 is only compatible with React 19`.

## Version 1.0

### Added
- Everything
